<div class="container">
  <div class="show-img">
    <%= cl_image_tag @venue.photos.first.key, crop: :fill %>
  </div>
  <div class="tags-box">
    <% @venue.tags.each do |tag| %>
      <p class="tag">#<%= tag.name %></p>
    <% end %>
  </div>
  <div class="review-photo-tgl">
    <h3>Reviews</h3>
    <h3>Photos</h3>
  </div>
  <div class="review-photo-dsply">
    <% @venue.reviews.each do |review| %>
      <div class="card-review">
        <%= cl_image_tag review.photo.key, crop: :fill %>
        <div class="card-review-infos">
          <!-- data migration to add review title -->
          <h2>Title goes here<!-- review.title --></h2>
          <p><%= review.comment %></p>
        </div>
      </div>
    <% end %>
  </div>
</div>
<% if policy(Review).create? %>
  <%= link_to 'Write Review', new_venue_review_path(@venue.id) %>
<% end %>

<!--make these radio buttons
    whichever is selected displays
    the content below -->
<!--form_for(@rev_foto, method: :get, remote: true) do |f| %>
      f.radio_button 'photos', {}, onchange: 'this.form.submit();', class: 'some-css-class' %>
      f.radio_button 'reviews', {}, onchange: 'this.form.submit();', class: 'some-css-class' %>
 end %> -->
